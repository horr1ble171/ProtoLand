---
import Container from '../common/Container.astro'

const projects = [
  { title: 'Luxe Beauty', tag: 'Интернет-магазин', color: '#e53935' },
  { title: 'SmartBuild', tag: 'Корпоративный сайт', color: '#1565c0' },
  { title: 'GreenLeaf', tag: 'Лендинг', color: '#2e7d32' },
  { title: 'CloudSync', tag: 'Веб-приложение', color: '#6a1b9a' },
  { title: 'FitStudio', tag: 'Сайт-визитка', color: '#e65100' },
  { title: 'ArtSpace', tag: 'Портфолио', color: '#00838f' },
]
---

<Container id="portfolio" className="section-gradient-reverse">
  <div class="text-center mb-12">
    <h2 class="text-3xl md:text-4xl font-bold mb-4" style="color: var(--text-primary)">Наши работы</h2>
    <p class="text-lg max-w-2xl mx-auto" style="color: var(--text-secondary)">
      Примеры проектов, которые мы реализовали
    </p>
  </div>

  <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-5 fade-stagger">
    {projects.map((p) => (
      <a
        href="#"
        class="group block rounded-2xl overflow-hidden border card-shadow transition-all duration-300 hover:-translate-y-2"
        style="background-color: var(--bg-card); border-color: var(--border)"
      >
        <div class="h-44 flex items-center justify-center transition-all duration-300 group-hover:scale-105" style="background: linear-gradient(135deg, {p.color}20, {p.color}08)">
          <span class="text-5xl font-black opacity-20 transition-all duration-300 group-hover:opacity-30 group-hover:scale-110" style="color: {p.color}">{p.title[0]}</span>
        </div>
        <div class="p-5">
          <span class="text-xs font-semibold px-2.5 py-1 rounded-full transition-colors duration-200" style="background-color: {p.color}20; color: {p.color}">{p.tag}</span>
          <h3 class="text-lg font-bold mt-2 group-hover:text-[var(--accent)] transition-colors" style="color: var(--text-primary)">{p.title}</h3>
        </div>
      </a>
    ))}
  </div>
</Container>